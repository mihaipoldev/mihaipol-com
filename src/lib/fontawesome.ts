import { config } from "@fortawesome/fontawesome-svg-core";

// Tell Font Awesome to skip adding the CSS automatically
// We're using the React component which renders SVGs directly
config.autoAddCss = false;

